
services:
  - type: web
    name: guardian-backend
    env: python
    buildCommand: cd backend && bash setup_django.sh
    startCommand: cd backend && bash start_render.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.2
      - key: DB_NAME
        fromDatabase:
          name: guardian-db
          property: database
      - key: DB_USER
        fromDatabase:
          name: guardian-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: guardian-db
          property: password
      - key: DB_HOST
        fromDatabase:
          name: guardian-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: guardian-db
          property: port
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: "guardian-vision.onrender.com,guardian-vision.vercel.app"
      - key: RENDER
        value: "true"

databases:
  - name: guardian-db
    databaseName: guardian_db
    user: guardian_user
    plan: free
    # Note: The database service ID is: dpg-d4u9587gi27c738kb790-a
    # Render will automatically inject connection variables via fromDatabase above
